# BANKPROJECT
This Java Servlet-based bank application handles user registration, login, deposit, withdrawal, balance checking, and profile updates using JDBC with MySQL. It processes HTTP requests, manages sessions, and dynamically interacts with the database to perform secure banking operations.
